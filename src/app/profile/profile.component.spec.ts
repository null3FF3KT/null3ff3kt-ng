import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ProfileNull3ff3ktComponent } from './profile.component';

describe('ProfileNull3ff3ktComponent', () => {
  let component: ProfileNull3ff3ktComponent;
  let fixture: ComponentFixture<ProfileNull3ff3ktComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [ProfileNull3ff3ktComponent]
    });
    fixture = TestBed.createComponent(ProfileNull3ff3ktComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
